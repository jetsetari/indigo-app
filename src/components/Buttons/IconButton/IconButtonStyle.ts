import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    paddingLeft: 0,
    paddingTop: 0,
    position: 'relative',
    zIndex: 5000
  },
  square: {
    width: 30,
    height: 30,
    borderRadius: 0,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
